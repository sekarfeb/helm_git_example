apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-nginx-app1.fullname" . }}
  namespace: default
  labels:
    {{- include "my-nginx-app1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-nginx-app1.selectorLabels" . | nindent 4 }}
